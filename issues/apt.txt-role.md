Create an apt.txt ansible role. This role goes through the ansible_parent_role_paths and searches for apt.txt files there. For each of those it just installs those apt packages, the default, or creates an apt.txt-$role.tmp.conf file for mkosi. The mkosi.configure calls ansible-playbook with a generated playbook file that corresponds to the mkosi profiles (you can get those over an environment variable).
