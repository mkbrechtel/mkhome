dependencies:

  # user setup
  - name: root_user
  - name: users
  - name: fish

  # network setup
  - name: network

  # system setup
  - name: hostname
    when: ansible_connection != "lxc"
  - name: locales
  - name: timezone
  - name: keyboard_configuration

  # packages setup
  - name: debian_repos
  - name: debian_packages
  - name: system_upgrade
