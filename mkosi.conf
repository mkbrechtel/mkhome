[Distribution]
Distribution=debian
Release=trixie

[Output]
Format=directory

[Config]
# Packages are automatically discovered from roles/*/apt.txt via mkosi.configure
ConfigureScripts=mkosi.configure

[Content]
PostInstallationScripts=mkosi.postinst

[Build]
ToolsTreePackages=ansible