{{ ansible_managed | comment }}

[Unit]
Description=barrier server app
PartOf=graphical-session.target
After=xsession-reload.service

[Service]
ExecStart=barrierc --no-daemon --enable-crypto --log %h/.local/share/barrier/barrierc@%i.log %i

[Install]
WantedBy=graphical-session.target
