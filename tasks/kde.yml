---
# tasks file for desktop

- name: install kde desktop
  apt: name={{ item }} state=latest
  with_items:
    - task-kde-desktop
    - task-german
    - task-german-desktop
    - kde-l10n-de
    - kio-mtp
    - gwenview
    - kipi-plugins
    - kde-config-gtk-style
    - qtcurve
